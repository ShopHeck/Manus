{
  "name": "trendz-backend",
  "version": "1.0.0",
  "description": "TRENDZ e-commerce intelligence backend — proxies Pinterest, Google Trends, Amazon",
  "main": "server.js",
  "scripts": {
    "build": "cd apps/web && npm install && npm run build",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
